import React from "react";
import NotFound from '../components/NotFound/NotFound';

const NotFound = () => {
    return (
            <>
                <NotFound />
            </>
    )
};

export default NotFound;